Reversion Reference Memo

This project was temporarily converted to a client-side mock sandbox for Meta App Review.

Original production architecture (to restore later):

- Supabase usage:
  - `supabase.from('chats')` and `supabase.from('messages')` used across `ChatList`, `ChatWindow`, and plugin components for live fetching and writes.
  - Real-time subscriptions via `supabase.channel(...)` for `chats` and `messages` to receive inserts/updates.
  - `supabase.auth.getSession()` and `supabase.auth.onAuthStateChange()` were used for session management.

- Server actions / API routes:
  - `/api/whatsapp/connect` and `/api/whatsapp/diagnostics` are server endpoints used by `BusinessSettings` for channel onboarding and diagnostics.
  - Some plugin components write directly to Supabase tables (e.g., messages, chats) when sending invoices, orders, or confirmations.

Temporary mock changes made for review recording:

- The main inbox view (`app/page.tsx`) was converted to a fully client-side sandbox using React `useState` only. All Supabase/auth calls in this view were removed and replaced by local mock state.
- A single hardcoded conversation "Customer One (WhatsApp Test)" was added with a pre-populated incoming message for the demo.
- Plugin actions were simulated client-side and append messages locally via a `handlePluginAction` callback.

To revert for production:

- Restore Supabase imports and the original data-loading `useEffect` calls in `app/page.tsx` (or re-wire `ChatList`/`ChatWindow` to fetch live data).
- Reconnect real-time subscriptions and remove the mock `handlePluginAction` triggers.
- Ensure the restored code uses secure server endpoints and retains previous auth/session checks.

Notes:
- Only the `app/page.tsx` file and this memo were changed. If you want, I can instead implement a non-destructive toggle (e.g., env flag) to switch between mock and live behaviour before reverting.
